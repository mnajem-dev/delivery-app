CLAUDE.md
node_modules

# Build output — generated by `pnpm -r build`, never commit
dist
**/dist
*.tsbuildinfo

# Local secrets / env — never commit these (keep .env.example tracked)
.env
**/.env
!**/.env.example

# Logs
**/*.log
